import { _decorator } from 'cc';

void _decorator;

/** UI 层级定义。 */
export enum UILayer {
  Background = 'Background',
  Scene = 'Scene',
  Window = 'Window',
  Dialog = 'Dialog',
  Toast = 'Toast',
  Loading = 'Loading',
  Top = 'Top'
}
